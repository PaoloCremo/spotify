import setuptools

setuptools.setup(
    version='0.1.0',
    name="spotify",
    packages=setuptools.find_packages(),
    install_requires=[
        #'',
        'pandas',
        'spotipy',
        'numpy',
        ]   
)
